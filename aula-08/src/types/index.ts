export type User = {
  id: number;
  name: string;
  username: string;
  email: string;
};

export type Product = {
  id: number;
  name: string;
  price: number;
};

export type ProductSelected = {
  id: number;
  quantity: number;
};
